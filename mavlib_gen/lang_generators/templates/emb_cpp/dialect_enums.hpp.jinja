/// All MAVLink enums for {{ dialect.name.lower() }}
///
/// AUTOGENERATED by mavlib_gen. DO NOT MODIFY DIRECTLY
#ifndef __MAVLINK_{{ dialect.name.upper() }}_ENUMS_HPP__
#define __MAVLINK_{{ dialect.name.upper() }}_ENUMS_HPP__

{% for enum in dialect.xml.enums %}
/// {{ enum.formatted_description(line_prefix="/// ") }}
enum {{ enum.name }} {
    {% for entry in enum.entries %}
    /// {{ entry.formatted_description(line_prefix="/// ") | indent }}
    {% if entry.has_params %}
    ///
    /// Param | Description
    /// ------|------------
    {% for param in entry.params %}
    /// {{ "%5s" | format(param.index) }} | {{ param.formatted_description(line_prefix="///       | ") | indent}}
    {% endfor %}
    {% endif %}
    {{ entry.name }} = {{ entry.value }},
    {% endfor %}
};

{% endfor %}
#endif /* __MAVLINK_{{ dialect.name.upper() }}_ENUMS_HPP__ */
